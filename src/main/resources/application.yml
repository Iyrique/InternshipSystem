server:
  port: 8080

spring:

  datasource:
    driver-class-name: org.postgresql.Driver
    password: postgres
    url: jdbc:postgresql://localhost:5432/InternshipDS
    username: postgres


  jpa:

    hibernate:
      ddl-auto: update

    show-sql: true
  flyway:
    table: flyway_schema_history